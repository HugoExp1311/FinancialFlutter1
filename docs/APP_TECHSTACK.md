# App Technology Stack

Generated from repository inspection on 2026-05-01. This file lists only technologies that are visible in the repository. External services that require setup outside the repo are marked as partial or prototype where the repository does not include deployment/configuration artifacts.

## Summary Table

| Area | Technology name | Role in the app | Exact files that prove it | Implementation status |
|---|---|---|---|---|
| 1. Frontend/mobile framework and language | Flutter with Dart | Main client application, UI screens, navigation, theming, and platform runners. | `app/pubspec.yaml`; `app/lib/main.dart`; `app/.metadata`; `app/android/`; `app/ios/`; `app/web/`; `app/windows/`; `app/macos/`; `app/linux/` | Fully implemented for the main client code. Platform targets are scaffolded, but production metadata still uses default names such as `flutter_application_1`. |
| 2. State management library | Riverpod (`flutter_riverpod`, `riverpod_annotation`, `riverpod_generator`) | Dependency injection, app providers, transaction stream state, computed totals, and chatbot message state. | `app/pubspec.yaml`; `app/lib/presentation/providers/app_providers.dart`; `app/lib/presentation/screens/chatbot_screen.dart`; Riverpod imports across `app/lib/presentation/screens/` and `app/lib/presentation/widgets/` | Fully implemented in the Flutter client. |
| 3. Local database | Isar | Offline-first local transaction storage, indexed transaction model, generated schema, and local CRUD/query tests. | `app/pubspec.yaml`; `app/lib/main.dart`; `app/lib/data/models/app_transaction.dart`; `app/lib/data/models/app_transaction.g.dart`; `app/lib/data/repositories/transaction_repository_impl.dart`; `app/test/isar_test.dart`; `app/isar.dll` | Fully implemented for local transaction storage. |
| 4. Cloud backend/database | Supabase, backed by a `transactions` table | Cloud sync target for transactions and cloud service used by the app. The code reads/writes `transactions` and stores `user_id` on synced rows. | `app/pubspec.yaml`; `app/lib/main.dart`; `app/lib/data/repositories/transaction_repository_impl.dart`; `app/lib/presentation/providers/app_providers.dart`; `My workflow11.json`; `My workflow11_MultiAgent.json`; `README.md` | Partially implemented in the repo. Client sync code and n8n Supabase nodes exist, but no Supabase SQL migrations, RLS policy files, or committed `.env` are present. |
| 5. Authentication method | Supabase Auth with email/password | User sign-up, login, session gate, current user lookup, and logout. | `app/lib/main.dart`; `app/lib/presentation/screens/login_screen.dart`; `app/lib/presentation/screens/auth_gate.dart`; `app/lib/presentation/screens/profile_screen.dart`; `app/lib/presentation/providers/app_providers.dart` | Fully implemented in the Flutter client for email/password sign-up, login, session switching, and logout. External Supabase project configuration is still required. |
| 6. AI/chatbot workflow tools | Flutter chatbot screen + n8n workflows | Chat UI sends user message, `user_id`, recent transaction history, and current date to an n8n webhook. n8n workflow routes requests, extracts transaction data, writes to Supabase, or returns analysis. | `app/lib/presentation/screens/chatbot_screen.dart`; `My workflow11.json`; `My workflow11_MultiAgent.json`; `build_n8n.py`; `README.md` | Partially implemented / prototype. The Flutter code uses a hardcoded local webhook URL `http://localhost:5678/webhook/ai-chat`; workflow credentials and active n8n hosting are external. |
| 7. LLM provider/model if visible | Groq Chat Model using `llama-3.3-70b-versatile` | LLM used inside the n8n workflow for supervisor routing, transaction extraction, and financial analysis. | `My workflow11.json`; `My workflow11_MultiAgent.json`; `build_n8n.py`; `README.md` | Partially implemented / prototype. The model is explicitly configured in workflow JSON, but Groq credentials and workflow activation are outside the repo. |
| 8. Optional backend/microservice stack | Dart backend using `shelf`, `shelf_router`, and shared `core_domain` package | Prototype REST transaction service with `/health`, `/transactions`, `POST`, `PUT`, and `DELETE` routes. Intended alternative to direct Flutter Isar/Supabase access. | `microservices/transaction_service/pubspec.yaml`; `microservices/transaction_service/bin/server.dart`; `microservices/transaction_service/lib/handlers/transaction_handler.dart`; `microservices/README.md`; `app/lib/data/repositories/transaction_repository_http.dart`; `packages/core_domain/pubspec.yaml` | Only planned/prototype. Server routes return stub/mock responses, and `TransactionRepositoryHttp` throws `UnimplementedError` for real data methods. |
| 9. Testing tools | `flutter_test`, Isar tests, Dart lints | Widget smoke test, Isar CRUD/query/aggregation tests, core-domain equality test, and static lint configuration. | `app/pubspec.yaml`; `app/test/widget_test.dart`; `app/test/isar_test.dart`; `packages/core_domain/pubspec.yaml`; `packages/core_domain/test/core_domain_test.dart`; `app/analysis_options.yaml`; `packages/core_domain/analysis_options.yaml`; `microservices/transaction_service/pubspec.yaml` | Partially implemented. Tests exist, but coverage is limited and does not cover Supabase sync, auth flow, chatbot workflow, statistics, or the microservice end to end. |
| 10. Deployment/platform targets | Flutter platform targets: Android, iOS, Web, Windows, macOS, Linux. Android Gradle/Kotlin, iOS/macOS Xcode/Swift, Windows/Linux CMake. | Standard Flutter project runners and build configuration for multiple platforms. Windows desktop sizing is explicitly handled in `main.dart`. | `app/.metadata`; `app/android/settings.gradle.kts`; `app/android/app/build.gradle.kts`; `app/android/app/src/main/kotlin/com/example/flutter_application_1/MainActivity.kt`; `app/ios/Runner.xcodeproj/project.pbxproj`; `app/ios/Runner/AppDelegate.swift`; `app/web/index.html`; `app/web/manifest.json`; `app/windows/CMakeLists.txt`; `app/linux/CMakeLists.txt`; `app/macos/Runner.xcodeproj/project.pbxproj`; `app/macos/Runner/AppDelegate.swift`; `app/lib/main.dart` | Partially implemented / scaffolded. Platform folders exist, but no CI/CD, Dockerfile, app store deployment config, or cloud deployment config was found. Web support is uncertain because `chatbot_screen.dart` imports `dart:io`, which is not available on Flutter Web. |

## Important Uncertainties

- Supabase database schema is described in `README.md` and implied by repository code, but there are no committed SQL migration files.
- Supabase RLS is described in `README.md`, but no policy files are present.
- `README.md` documents `N8N_WEBHOOK_URL`, but `app/lib/presentation/screens/chatbot_screen.dart` currently hardcodes the local n8n URL.
- The optional microservice is not the active data path. `app/lib/presentation/providers/app_providers.dart` currently returns `TransactionRepositoryImpl(isar, supabase)`.
- No production deployment artifacts were found. Repository evidence only confirms generated Flutter platform targets and local run instructions.

